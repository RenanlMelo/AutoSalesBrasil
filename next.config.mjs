/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['cdn.sanity.io', 'https://localhost:3000', 'https://localhost:3333', 'https://autosalesbrasil.sanity.studio']
   }
}

export default nextConfig;
